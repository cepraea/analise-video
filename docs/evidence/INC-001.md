# INC-001 — Evidência consolidada do architecture spike

- Estado do gate técnico: **VERIFIED** localmente; CI **success**.
- Resultado arquitetural registrado em [ADR-001](../architecture/ADR-001.md): **PASS WITH RISKS**.
- Branch: `inc-001-architecture-spike`.
- Commit de código testado: [`23336f737e3ea3cccd8bfe62ef6dc1e83cfffa30`](https://github.com/cepraea/analise-video/commit/23336f737e3ea3cccd8bfe62ef6dc1e83cfffa30).
- PR: [#1, em rascunho](https://github.com/cepraea/analise-video/pull/1).

## Vídeo usado sem versioná-lo

| Campo | Valor observado |
| --- | --- |
| Nome local | `CEPRAEA x COSTA.mp4` |
| Caminho | `.local/media/CEPRAEA x COSTA.mp4` (`.local/` é ignorado pelo Git) |
| Tipo | Contêiner MP4; vídeo H.264 e áudio AAC, conforme `ffprobe` |
| Duração | `2182.055` s (`36:22.055`) |
| Tamanho | `854101055` bytes |

O arquivo real e o SQLite `.local/data/inc001.sqlite3` não integram o commit. A CI verifica código e testes determinísticos; o comportamento perceptivo foi observado no navegador local com esse MP4.

## Integridade do original — RF-030/RNF-005

| Medição | SHA-256 |
| --- | --- |
| Antes da marcação, registrado em [INC-001-save.md](INC-001-save.md) | `ea6607f4d6a0095c1b413722fefa41aa50be57f2e1b6b9bed1fef7ed908c23c9` |
| Depois de marcação, salvamento, reinício, revisão e demais testes; recalculado em 2026-09-14 | `ea6607f4d6a0095c1b413722fefa41aa50be57f2e1b6b9bed1fef7ed908c23c9` |

**Resultado:** hash antes = hash depois. `sha256sum --check` retornou `OK` com código `0`; a listagem de `.local/media` mostrou somente o original. A comparação detalhada está em [INC-001-original-integrity.md](INC-001-original-integrity.md). Essa evidência vale para o arquivo e o incremento testados; RF-030/RNF-005 continuam a exigir verificação nos incrementos futuros.

## Comandos executados

Na raiz do repositório, para teste e build completos:

```bash
make test
make build
```

Para observar a aplicação local, o backend foi iniciado com `.venv/bin/uvicorn cepraea_video.main:app --app-dir backend/src --host 127.0.0.1 --port 8000` e o frontend com `cd frontend && npm run dev -- --host 127.0.0.1`. Ambos foram encerrados e iniciados novamente no teste crítico; o navegador também foi fechado e reaberto. A inspeção do arquivo e da integridade usou:

```bash
ffprobe -v error -show_entries format=format_name,duration,size -show_entries stream=codec_type,codec_name -of json '.local/media/CEPRAEA x COSTA.mp4'
sha256sum '.local/media/CEPRAEA x COSTA.mp4'
printf '%s  %s\n' 'ea6607f4d6a0095c1b413722fefa41aa50be57f2e1b6b9bed1fef7ed908c23c9' '.local/media/CEPRAEA x COSTA.mp4' | sha256sum --check
find .local/media -maxdepth 1 -type f -printf '%f\n'
```

## Testes automatizados e CI

| Verificação | Resultado |
| --- | --- |
| `make test` — pytest, incluindo `/health` e diretórios do INC-000 | **7 passed**; 2 advertências de depreciação, sem falha |
| `make test` — Vitest, incluindo metadados do frontend do INC-000 | **2 arquivos, 5 testes passaram** |
| `make build` — TypeScript + Vite | **passou** |
| [CI run `34796462010`](https://github.com/cepraea/analise-video/actions/runs/34796462010), evento `pull_request`, no commit acima | **completed / success**; pytest 7, Vitest 5, build passou |

O workflow usou Python `3.12` e Node `22.12.0`, de acordo com os manifests. Os testes determinísticos cobrem criação, rejeição de `start_ms >= end_ms`, reabertura do SQLite, recuperação e comandos básicos da interface; ver [INC-001-deterministic-tests.md](INC-001-deterministic-tests.md). Eles não substituem a observação do vídeo real.

## Resultados dos testes no navegador com MP4 real

As interações foram dirigidas passo a passo no Chromium por `agent-browser`, com inspeção da tela e dos estados/eventos do player. Não houve upload nem serviço de vídeo externo.

| Teste | Observação | Resultado |
| --- | --- | --- |
| Abrir mídia local | MP4 listado e carregado no player; duração `2182.055` s; requisições de mídia por faixa retornaram `206`; sem overlay de erro. | **Passou** |
| Play, pause, seek para frente e para trás, tempo corrente | `play` avançou o tempo; pausa em `23.208` s; avanço de 10 s para `33.208` s; retorno de 10 s para `23.208` s; tempo visível na interface. | **Passou** |
| Marcação e validação | `INICIAR LANCE` capturou `75.320` s (`75320` ms); `ENCERRAR LANCE` capturou `84.210` s (`84210` ms). Fim anterior ou igual ao início foi recusado com erro visível. | **Passou** |
| Salvar clip virtual | `POST /spike/intervals` retornou `201`; intervalo 001 apareceu na interface e no SQLite com `75320`–`84210` ms. Nenhum MP4 derivado foi criado. | **Passou** |
| Rever intervalo salvo | Depois de reabrir a aplicação, `REVER` buscou `75.320` s, iniciou `play` e pausou em `84.210` s; a repetição também concluiu. | **Passou** |
| Teste crítico completo de persistência com novo registro | Pela interface, intervalo 002 marcado e salvo em `10.000`–`20.000` s (`201`). Backend, frontend e navegador foram encerrados; as portas ficaram livres. Novos processos e uma nova sessão recuperaram o registro via `GET /spike/intervals` (`200`). `REVER` gerou `seeked` em `10.000` s, `playing` com `paused=false`, e terminou em `20.000` s com `paused=true`. | **Passou** |
| Preservação do original após todos os testes | SHA-256 final idêntico ao inicial; `sha256sum --check: OK`; somente o MP4 original em `.local/media`. | **Passou** |

Detalhes das sessões estão em [INC-001-player.md](INC-001-player.md), [INC-001-marking.md](INC-001-marking.md), [INC-001-save.md](INC-001-save.md) e [INC-001-review.md](INC-001-review.md). A parada da revisão tem precisão temporal funcional; precisão frame a frame e avaliação humana independente da experiência não foram comprovadas. O SQLite `spike_intervals` é descartável e não define o schema canônico de `LANCE`. Por isso, o gate recebeu **PASS WITH RISKS**; a proposta do ADR-001 continua **PROPOSTA A VALIDAR**.
