# Evidência — INC-000 Fundação executável

- Estado: `VERIFIED`
- Data da verificação: 2026-09-13
- Branch: `main`
- Commit verificado pela primeira execução completa de CI: `764e0602836462f6cc75bc73885594ab8aaa7918`
- GitHub Actions run: `34782679938`
- Job: `INC-000 foundation`
- Conclusão: `success`

## Critério do incremento

O `INC-000` exige estrutura mínima reproduzível para desenvolvimento e testes, sem comportamento esportivo novo; frontend/backend compatíveis com a hipótese do ADR-001; comandos documentados; configuração local sem segredo pago obrigatório; dados locais fora do versionamento; teste mínimo de inicialização.

## Evidências observadas

Na execução limpa do GitHub Actions:

1. checkout do repositório: `success`;
2. Python 3.12 configurado: `success`;
3. instalação do backend: `success`;
4. testes do backend: `success`;
5. Node 22.12 configurado: `success`;
6. instalação do frontend: `success`;
7. testes do frontend: `success`;
8. build do frontend: `success`.

## O que foi comprovado

- O pacote backend pode ser instalado em Python 3.12.
- A aplicação FastAPI inicializa e responde ao health check testado.
- Os diretórios locais de runtime podem ser criados sem cloud ou segredo obrigatório.
- O frontend React/TypeScript instala, executa seu teste mínimo e compila com Vite.
- Os comandos e pré-requisitos estão documentados no `README.md` e `Makefile`.
- `.local/`, artefatos Python, dependências/build do frontend e arquivos `.env` locais estão fora do versionamento.

## O que NÃO foi comprovado neste incremento

Não foram implementados nem validados: reprodução de vídeo, captura de timestamps, SQLite de domínio, `VideoSource`, `LANCE`, persistência de intervalo, fases, atletas, ações, filtros, coleções, FFmpeg ou fluxo funcional completo.

O `ADR-001` permanece `PROPOSTA A VALIDAR`; sua validação arquitetural pertence ao `INC-001` com vídeo local real.
